// login.js — entry point Vite exclusivo do gateway de login compartilhado. Só importa
// o CSS (Bootstrap 3.3.5 é puramente decorativo aqui — grid/.form-control/glyphicons,
// nenhum plugin JS do Bootstrap é usado nesta tela, o legado real também não usa
// nenhum na tela de login além de iCheck, que é só cosmético de checkbox e não afeta
// fidelidade visual do formulário em si).
import '../../sass/identidade/login.scss';
